# Add this secret in Vercel Project Settings > Environment Variables.
# Never commit the real value.
STUDY_NOTION_API_KEY=

# Optional database overrides. Defaults are already configured server-side.
# STUDY_COURSES_DB=
# STUDY_TASKS_DB=
# STUDY_SESSIONS_DB=
# STUDY_CLASSES_DB=
