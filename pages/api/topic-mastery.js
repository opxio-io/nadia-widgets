const NOTION_VERSION='2022-06-28',DB='5d3117af-6452-4457-a2c5-b8b68798ce54';
const map={learned:'Learned?',practiced:'Practiced?',alone:'Can Do Alone?',review:'Needs Review?'};
export default async function handler(req,res){if(req.method!=='PATCH')return res.status(405).json({error:'Method not allowed'});const token=process.env.NOTION_TOKEN||process.env.NADIA_NOTION_API_KEY||process.env.STUDY_NOTION_API_KEY||process.env.NOTION_API_KEY;if(!token)return res.status(503).json({error:'Notion API key missing'});let {id,confidence,...vals}=req.body||{};if(!id)return res.status(400).json({error:'Missing topic id'});let properties={};for(const [k,n] of Object.entries(map))if(typeof vals[k]==='boolean')properties[n]={checkbox:vals[k]};if(confidence)properties.Confidence={select:{name:confidence}};try{let r=await fetch('https://api.notion.com/v1/pages/'+id,{method:'PATCH',headers:{Authorization:'Bearer '+token,'Notion-Version':NOTION_VERSION,'Content-Type':'application/json'},body:JSON.stringify({properties})});if(!r.ok)throw Error(await r.text());return res.status(200).json({ok:true})}catch(e){return res.status(500).json({error:'Could not update Notion'})}}